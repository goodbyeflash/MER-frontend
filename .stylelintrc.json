{
  "extends": "stylelint-config-standard",
  "rules": {
    "no-descending-specificity": null,
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["mixin", "include", "extend", "selectors-within-list"]
      }
    ]
  }
}
